Canvas Playground
---------------------------

canvas editor with konva, react and redux

    git clone https://github.com/OssamaZ/canvas-play.git
    cd canvas-play
    npm install

start `npm start`

build `npm run build`
